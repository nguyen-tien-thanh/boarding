version: '3.8'

services:
  boarding:
    image: nguyentienthanh2909/371-boarding:latest
    container_name: boarding
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - RABBITMQ_URL=${RABBITMQ_URL}